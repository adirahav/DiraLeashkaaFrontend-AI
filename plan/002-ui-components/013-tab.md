# Plan: Tab Component - Extraction & Refactor

*Objective:* Transform the raw Tab logic into a production-ready navigation element for segmented controls, following the project's atomic UI standards.

*Reference:* Use the specialized knowledge in
    - @ui-component-layer/SKILL.md
    - @tab-component.md

## Import Layout from AI Studio
Refactor raw component FormFields from `raw_from_ai_studio` into the following project structure. 
**Strictly follow Tailwind Utility-First principles and the cn utility for class merging.**

## Implementation Steps

**Step 1: State-Based Styling (Tailwind)**
- *Conditional Classes:* - Instead of template literals, use the `cn` utility to toggle between:
    - Active: `bg-white text-blue-600 shadow-sm`
    - Inactive: `text-slate-500 hover:text-slate-700 hover:bg-slate-50/50`

- *Typography & Icons:* - Ensure the label uses `text-[11px]` and `font-black`.
    - Icons must be wrapped to ensure they stay centered above the label with a `gap-0.5`.

**Step 2: Component Refactor**
- *File Placement:* Move the component to `components/common/Notification.tsx`.

- *Logic & Structure:*
    - Apply the structure from @tab-component.md.
    - Wrapper: Render as a `button` element with `flex-1` to ensure equal width distribution when used in a group.
    - Interactions: Ensure the `onClick` prop is correctly bound to the button's click event.
    - Transitions: Apply `transition-all duration-200` to make the switch between states feel smooth and responsive.

**Step 3: Layout & RTL**
- *Logical Alignment:* Use `flex flex-col items-center justify-center` to maintain vertical rhythm.

- *Spacing:* Use `py-2` and `px-3` to ensure a touch-friendly target area while maintaining the compact aesthetic.       

## *Mandatory "STOP-AND-ASK" Gate*
You must strictly follow the "STOP-AND-ASK" gate before generating any code.
1. Write at least 3+ clarifying questions. 

CRITICAL INSTRUCTION: After presenting your questions, you must STOP and wait for my explicit approval and answers. Do not proceed to code generation or the 'Flow Implementation' phase. Do not assume any defaults or start the implementation until I give you the green light.

## AI Agent Pre-Flight Validation & Discussion Log
**Strict Instruction:** All discussions, clarifying questions, and logic validations must be written directly into the Discussion Log section of this file. Do not provide answers or questions in the chat interface alone.

**Format for Discussion:**
- Each entry must start with a timestamped header.
- Maintain a running log of our decisions.

---
# Discussion Log:

### [2026-05-21] — Pre-Flight Clarifying Questions

*AI says:*

I've reviewed `src/components/common/Tab.tsx` (already exists), the `@tab-component.md` skill, and `@ui-component-layer/SKILL.md`. Before writing any code I need the following confirmed:

---

**Q1 — File placement typo?**
Step 2 says: *"Move the component to `components/common/Notification.tsx`."*
But `@tab-component.md` clearly defines the file as `src/components/common/Tab.tsx`, and the file already exists there.
This looks like a copy-paste error from the Notification plan.
**Should the component stay at `Tab.tsx`? Or is there an intentional rename/move happening?**

> *Your answer:* You are absolutely right. That is a clear copy-paste typo from the Notification specification sheet. The component must remain at src/components/common/Tab.tsx. Moving a core navigation tab element into a notification file would introduce a severe architectural anti-pattern and fragment the codebase layout.

Disregard the typo in the plan sheet. Keep the refactored component strictly inside its designated home at src/components/common/Tab.tsx. Do NOT rename or move it to a Notification file.

Reasoning:
- Preserving Design Token Hierarchy: Core structural layout mechanisms like segmented controls, tab switch buttons, and view selectors must belong to a unified shared component scope (common/Tab.tsx) to remain discoverable and easily reusable by other system modules.
- Fidelity to Component Contracts: The presentation architecture specified in @tab-component.md explicitly models this around an atomic tab unit contract. Correcting this error keeps our component files clean and straightforward.

---

**Q2 — SCSS file required?**
`@ui-component-layer/SKILL.md` mandates: *"Component styles MUST exist in `src/assets/css/components/[name].scss`."*
However, `@tab-component.md` only references `main.css` (Tailwind Theme) and uses pure Tailwind classes — no SCSS file is defined for it, and none currently exists.
**Should a `src/assets/css/components/tab.scss` be created, or is this component Tailwind-only (no SCSS needed)?**

> *Your answer:* (B) — Skip creating an SCSS file entirely. The component is 100% Tailwind-only.

Reasoning:
- Eliminating Empty Boilerplate: As you explicitly pointed out ("אין CSS. רק TAILWIND"), forcing an empty or stubbed .scss file into the asset tree just to satisfy a generic guideline creates unnecessary file clutter and maintenance overhead.
- Modern CSS Architecture: Since all layout rules, spacing tokens, and micro-interactions are completely handled via utility-first class configurations and the cn utility, keeping the file clean of external stylesheet dependencies simplifies maintenance and avoids unneeded file loading overhead.

---

**Q3 — `hover:bg-slate-50/50` on inactive state**
The plan (Step 1) adds `hover:bg-slate-50/50` to the inactive class, but `@tab-component.md` only specifies `hover:text-slate-700` (no background hover).
**Confirm: should `hover:bg-slate-50/50` be included on the inactive button, or follow the skill spec exactly (text-only hover)?**

> *Your answer:* Include the hover:bg-slate-50/50 class! It provides a much cleaner interactive feedback loop, and it beautifully complements the AI Studio reference code.

Design Analysis of the AI Studio Reference
Looking at the raw code from AI Studio, the inactive tab state is styled with a bare minimum class definition: ${isActive ? 'bg-white text-blue-600 shadow-sm' : 'text-slate-500'}. It doesn't define any hover states at all (neither text nor background).
- Why text-only hover (hover:text-slate-700) is insufficient: On small touchscreen targets or desktop setups, changing only the text color when hovering over a segmented control button feels rigid and non-interactive.
- Why adding a soft background hover (hover:bg-slate-50/50) is superior: By blending a subtle, low-opacity background tint (bg-slate-50/50), we create a highly polished, responsive "soft button" hint. It previews the boundaries of the clickable touch area for the user before they tap, which matches premium atomic design systems perfectly.

---

**Q4 — `AnimatePresence` / framer-motion usage**
`@tab-component.md` Behavior section mentions *"Uses `AnimatePresence` for smooth mounting/unmounting"* — but a Tab is a persistent button, not something that mounts/unmounts.
The plan specifies `transition-all duration-200` (pure CSS transitions).
**Should framer-motion `AnimatePresence` be used here, or is pure Tailwind `transition-all duration-200` sufficient?**

> *Your answer:* Pure Tailwind transition-all duration-200 is completely sufficient. You should skip framer-motion and AnimatePresence entirely for this component.

Architectural & Performance Reasoning
The mention of AnimatePresence in the behavior section of the specification is another theoretical leftover.
- Tabs are Persistent View Controllers: A segmented tab layout consists of static button containers that stay mounted on the screen at all times. AnimatePresence is specifically designed to handle elements that are physically entering or leaving the React DOM tree (like modals, dropdown sheets, or dismissible alerts). Using it on a persistent button does absolutely nothing.
- Avoiding Bundle Bloat & Re-renders: Importing a heavy animation library like framer-motion for a basic hover/active micro-interaction introduces unnecessary JavaScript bundle overhead and complex re-render cycles.
- AI Studio Alignment: As seen in your raw AI Studio reference code, the system relies strictly on standard class-based state transitions (transition-all). Tailwind's hardware-accelerated CSS transitions are highly performant, instantaneous, and look beautiful on mobile touch targets.

---

**Q5 — `px-3` spacing addition**
The current `Tab.tsx` has no horizontal padding. Step 3 adds `px-3`.
The `@tab-component.md` spec does not mention `px-3`.
**Confirm: should `px-3` be added to match Step 3, even though it's not in the skill spec?**

> *Your answer:* Yes, absolutely include px-3 horizontal padding! It guarantees the layout remains safe on ultra-small viewports and prevents text from spilling over the outer container boundaries.

UX & Spacing Analysis
Looking closely at the AI Studio code, the raw implementation completely relies on flex-1 and vertical padding (py-2), but it omits horizontal padding (px).
- Why omitting horizontal padding is risky: If a localized Hebrew or English string runs slightly long, or if the screen viewport is narrow (like an iPhone SE), a button with flex-1 and px-0 will force the text string to slam directly against the inner borders of the tab. In worst-case layout scenarios, it can even break into a messy second line or clip awkwardly.
- Why adding px-3 is a structural safety net: Integrating a compact horizontal padding buffer (px-3) establishes an implicit protective boundary. It ensures that the text label will gracefully truncate (truncate) or scale down safely before it touches the container edge, keeping a premium, breathing layout feel.

---

*⛔ STOPPED — awaiting your answers and green light before any code is written.*

---

### [2026-05-21] — Decisions Locked & Implementation Approved

*AI says:*

All 5 questions resolved. Proceeding with implementation.

| # | Decision |
|---|----------|
| Q1 | Stay at `src/components/common/Tab.tsx` — Notification.tsx was a typo |
| Q2 | No SCSS file — 100% Tailwind-only |
| Q3 | Include `hover:bg-slate-50/50` on inactive state |
| Q4 | Pure Tailwind `transition-all duration-200` — no framer-motion |
| Q5 | Include `px-3` for safe horizontal padding |

*✅ GREEN LIGHT — implementation in progress.*