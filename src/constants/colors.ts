// Canonical hex values for design tokens defined in main.css @theme.
// Use these wherever a raw hex string is required (e.g. native StatusBar API).
export const COLORS = {
  white: '#ffffff',
  secondary: '#2563eb',   // --color-secondary / --color-logo-secondary (blue-600)
} as const
